# Pony Journey

Browser / PWA equestrian life game. Production: https://pony-journey.netlify.app

Static site. `netlify.toml` publishes `.` with no build command.

## Local
Open `index.html` via a static server (ES modules).

## Deploy
GitHub `DigitalDevAI/pony-journey` → `main` → existing Netlify site `pony-journey`.
